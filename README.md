# reviewgate-smoke
Clean-room smoke tests for ReviewGate releases

This repository validates the moving `v0` action tag from a clean-room installation.

Release smoke validated: `v0.8.1` collects opt-in ephemeral semantic repository context from the exact checked-out head without persisting source excerpts.
